[package]
name = "cycle-paradox-extension-api"
version = "0.1.0"
edition = "2024"

[dependencies]
bincode-next = { version = "3.1.1", features = ["serde"] }
serde = { version = "1.0.229", features = ["derive"] }
